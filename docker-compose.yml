version: '3.8'

services:
  goch-website:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: goch-website
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production

