export function BioBlock() {
  return (
    <section className="bio-block" aria-labelledby="bio-heading">
      <h2 id="bio-heading" className="section-label">
        About
      </h2>
      <p>
        Software engineer and designer based in Skopje. I build minimal,
        considered products at the intersection of craft and code.
      </p>
    </section>
  );
}
